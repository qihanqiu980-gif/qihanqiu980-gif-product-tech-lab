# 学习资料更新记录

## 2026-08-21：W1D5 Network 证据观察课程

- 用户亲自确认 Day 04 后，仅新增并开放 W1D5；inventory 由真实门禁更新为 5/72，W1D6 仍未创建或开放。
- W1D5 复刻正式 Day 01 工作台、两时段七章、sticky／移动目录和学习—练习—复述状态，只写 Network 请求条目与证据边界的独立正文。
- 最终门禁与离线构建通过；`index.html` 为 7,477,010 bytes，SHA-256 `3781301f3d127c853259364998a2b6d37507426d0fba1d716ee969d98ea090ba`，外部运行资源 0。当前继续等待用户亲自确认 Day 05。
- 专属教学观察器覆盖正常、4xx、5xx、慢 Timing、Size、Initiator 与缓存七类脱敏变式，所有请求、响应、耗时与大小均明确为教学模拟。
- 今日成果为可复现的 Network 观察记录，固定“看到／可以推断／还不能证明／下一步”，不凭单条请求证明后端实现或数据库事实。
- 草稿、追加式历史尝试、证据账本与 D1/D3/D7/D14/D30/D60 复习排程继续使用正式 v2 状态源；确认前不得开始 Day 06。

## 2026-08-12 · W1–W2 HTML 样板 v1

- 新增 W1 请求链路、模拟 Network、即时小测和工作迁移链路图。
- 新增 W2 鉴权、权限、缓存、后端异常故障注入与 Bug 报告实验。
- 新增 12 周路线、技术词典、能力矩阵与 D1/D3/D7/D14/D30/D60 复习队列。
- 学习状态由网页本地保存，可导出 JSON 档案与 ICS 日历。
## 2026-08-12：课程深度升级

- 12周课程由概览清单升级为72个学习单元。
- 每周固定包含理解、读图、实验、诊断、评审、迁移6课。
- 新增48个业务场景挑战、24个实验/诊断、72道初版开放检测题和评分标准；后续升级见下文。
- W1–W2即时选择题由每周3道扩展至每周6道，共12道，并保留错题复训与间隔复习。
- 全局正文基线调整为16px，展示标题改用清晰的Noto Sans SC，增大行距和模块间距。
## 2026-08-12：全 12 周深度课程升级

- 所有 12 周已开放完整课程入口。
- W3–W12 新增业务场景、因果链、实验起始材料、6 步实验、证据清单、易错点与作品模板。
- 每个单元从 1 道开放题扩展为基础回忆、场景应用和变式挑战 3 层练习，共 216 道。
- 即时选择题从 12 道扩展为 72 道，覆盖全 12 周。
- 统一提高词典、复盘、进度和选择题的正文与辅助字号。
# 2026-08-13

- W3–W12 新增 10 个可下载 ZIP 实验资料包，所有数据均为教学模拟与脱敏数据。
- SQL、指标、JOIN、API、权限/幂等、Python、pandas、A/B 实验、AI 评测与作品集均增加起始文件、运行步骤、答案或自检标准。
- W7 新增 20 项验收 Excel 矩阵；W11 新增 20 条 AI 固定回归样本和公式评分 Excel。
- 完整课程正文、代码和选择题字号提高，并增加实验资料的渐进披露入口。

## 2026-08-13：课程深度框架与题库升级

- 每周新增“概念与边界—机制与链路—证据与实验—失败与评审”四层知识框架，以及三个关键问题、进阶任务和研发术语。
- 每课由 3 层练习升级为术语边界、机制推演、证据判读、场景应用、变式评审 5 层，共 360 道开放练习。
- 每周即时选择题由 6 道扩展为 10 道，共 120 道，新增架构、数据偏差、兼容、并发、实验与 AI 安全等高阶判断。
- 课程路线由三列密集卡片调整为两列舒适阅读，并提高正文、练习、证据、按钮与辅助文字字号。
- 已取得用户提供的视频并按时间戳完成结构审计；课程统一采用“为什么学—概念关系—交互观察—产品案例—代码实验—快速自测—记忆清单—下一步”的八段教学结构，详细证据见《视频框架映射.md》与视频证据截图。

## 2026-08-13：本地空白页修复

- 根目录 `index.html` 改为内嵌课程脚本与样式，避免 Chrome 从 `file://` 页面加载外部资源时出现空白页。
- 新增中文入口 `打开课程.command`，可通过本地服务器稳定启动完整课程。
- 启动器改为默认使用固定端口 4317 并打开内嵌资源入口；端口冲突时明确提示，避免学习状态因静默换端口而分散。需要时可显式设置 `COURSE_PORT`。
- 启动脚本在已有 `dist` 构建产物时不再要求安装前端依赖，离线使用更稳健。
- 页面初始化失败时会显示具体恢复方法，不再停留在无提示白屏。

## 2026-08-13：视频教学框架对齐升级

- 归档参考视频元信息、时间戳、联系表和 6 张代表性关键帧，明确只迁移视频实际展示的教学设计。
- 12 周完整课程统一增加“为什么学—概念关系—交互观察—产品案例—代码实验—快速自测—记忆清单—下一步”顺序。
- W3–W12 新增 10 个页面内实时观察器，覆盖 SQL 边界、指标口径、JOIN 粒度、API 契约、幂等并发、Python 执行、数据清洗、A/B 实验、AI 评测和作品集证据。
- 每个观察器包含 3–5 个可调变量、动态指标、可视化条形证据和随条件变化的产品判断。
- 完整课程新增 7 项锚点导航、5 条闭卷记忆清单和下一周承接说明。
- 全局基础字号提高到 17px，完整课程正文提高到 18px，并扩大页面和模块留白。
- 最终可读性审计进一步将所有可见辅助与测量文字提高到至少 12px，并加深浅色页面的弱文字颜色，使辅助信息也达到清晰可读标准。

## 2026-08-13：W1D1 内容质量基准冻结

- W1D1 升级为独立日课：12 段结构、12 个完整概念、11 步教师示范、8 步引导实验、独立变式、4 道专属练习、15 字段成果教学与六阶段复习。
- 12 张概念卡需要逐张阅读并点击卡内首学确认；阅读完成与真正掌握分开记录。
- 系统实操证据只覆盖 JavaScript、DOM、用户事件和页面状态；解释、独立变式、成果与迁移继续等待量规或审核。
- 成果新增引导模板与空白独立练习版，切换覆盖草稿前确认；有效正文排除未修改模板、ATX/Setext Markdown 标题和纯标点。
- W1D1 完整证据可备份与局部恢复；恢复前确认，只替换 W1D1，并在跨 Day 引用不安全时拒绝操作。

## 2026-08-13：全项目审计与证据基础设施

- 新增 91 项全项目验收矩阵和 72 Day 逐日依赖盘点；明确当前真实完整日课为 1/72，其余 71 Day 仍是旧提纲。
- 新增完整日课权威注册表与 `npm run audit:course-inventory`，避免把可解析路由、标题或机械练习误报为完成课程。
- v2 账本新增追加式成果/解释/变式审核记录；审核引用不可变尝试，支持修订后的新审核覆盖旧结论而保留历史。
- 收紧复测：必须提交与任务、Day、概念、阶段和到期日匹配的 `spaced-retest`；D1/D3 不满足长期保持，迁移证据必须改变真实场景维度。
- 新增 `npm run validate:evidence`，并重新通过 W1D1 21/21、TypeScript、正式构建与启动脚本语法检查。
- 日课入口改为注册表驱动；W1D2–W12D6 未完成时显示诚实的质量门槛页面，不再出现空白页或错误继承 W1D1 沙盒。
- `DailyCourse` 新增跨日 `prerequisiteConceptIds`，并加入首次教学冲突、未来前置和重复练习验证。
- 复盘页新增 v2 真实复测执行台：闭卷提交、追加式量规审核、修订版本保留、D1/D3 与长期保持分离均已接入。
- `npm run validate:w1d1` 21/21、TypeScript 检查、正式构建、静态 HTTP 哈希核对和启动脚本冒烟均通过。

## 2026-08-14：证据审核、真实进度与概念索引

- 复盘页新增统一待审核证据工作台，覆盖闭卷解释、独立变式、今日成果和工作迁移；量规结论追加到 `AssessmentRecord`，不改写原始提交。
- 同一活动只把最新修订放入活动审核队列，旧提交与旧审核继续保留在账本；工作成果 80 分通过，其他核心量规要求关键维度全部满足。
- 进度页改为权威 `DailyCourse` 注册表＋v2 账本：单独展示 1/72 内容覆盖、真实日课完成状态、证据类型覆盖与 L0–L3 概念矩阵。
- “技术词典”升级为真实概念索引，连接首次教学 Day、本课前置、完整定义、易混边界、本课实验、错题、当前掌握证据与下一次复习。
- 注册表新增概念唯一性约束和 `listRegisteredConcepts()`，防止重复 ID 造成 Vue key 冲突、证据串线或能力百分比分母失真。
- 新增证据审核队列回归测试；TypeScript、证据 v2、课程目录与 W1D1 结构校验均重新通过。
- W1D1 教师示范与教学沙盒统一为 17/18 岁报名边界案例，安全 Console 支持比较与字符串拼接，模拟刷新会恢复标题、年龄与按钮状态。
- 独立变式新增单独预测字段，并将预测、计划、证据、结果和结论的 UI 完成门槛与 evidence adapter 对齐；闭卷解释统一为至少 40 字。
- 通用 `validateDailyCourse()` 与 W1D1 体量基准拆分：普通 Day 以教学完整性和引用一致性为底线，W1D1 继续由专属校验冻结，避免为其余 71 Day 机械制造数百个概念；最新又补入独立反馈段、失败纠错门槛和全题型错题入账检查，当前为 27/27。
- 72 Day 通用门禁进一步收紧：Day ID 必须与 week/day 一致，下一课不能跳序，课程内外 ID 必须唯一，每个首次教学概念都要进入实验、练习、成果或闭卷证据，30/45 分钟路径不得漏掉引导实验必修步骤。
- 课程目录验证改为直接读取权威注册表，并加入字符三元组相似度检查；只替换浏览器名、Day 名或术语的近似练习会自动失败。盘点脚本也直接读取注册表，不再依赖源码正则猜测已注册课程。
- 内容注册与实现注册分离：只有同时具备已审核渲染器、领域实验适配器和 v2 证据适配器的 Day 才计入可学习数量；`validate:course-complete` 与 `build:release` 在 72/72 前保持失败。
- 通用日课门禁新增机械占位反例：阻止重复概念正文、重复成果字段/修订/自检、重复记忆与复习任务、重复单选项、一道题承包五类标签、空补课步骤，以及 45 分钟路径换掉而非包含 30 分钟任务。
- 实现注册表改为保存真实 Vue 加载器、可执行实验契约和可执行证据契约；`validate:course-implementations` 会通过 Vite SSR 实际加载 W1D1 渲染器，阻止 Day 错配、空加载器或伪实现对象。
- 新增 `validate:day-routes`，覆盖全部 72 个日课哈希、无效深链和返回 Today 行为；当前报告为 72 个预期路由、1 个可学习、0 个实现待接入、71 个内容缺失。
- 首页明确提供今日 W1D1、继续学习、复盘/证据审核、目标、时间与真实掌握；“12 周课程路线”移除规模宣传、参考视频对齐和机械五层练习入口，所有 Day 都进入真实日课或诚实的开放条件页。
- 生产构建将真实动态日课渲染器内联为单一 JS，把 `import.meta.url` 转为离线经典脚本可用的基准地址，并将 CSS 引用的 101 个 WOFF2 字体子集改为 data URL；连续两次构建稳定得到 6,738,225 bytes、SHA-256 `91f90c53c6cffde3ba2217877f9fd7a7d8ab3358b2a70599ac5072c2408bc164`。
- 新增 `validate:offline-single-file` 并接入 `build` 与 `validate:all`：验证外部运行资源为 0、内嵌脚本可由经典脚本解析、没有模块导入、相对 CSS 资源或旧路径残留。
- 使用 `COURSE_PORT=4328 COURSE_NO_OPEN=1 ./start-course.command` 完成真实启动冒烟：脚本按需构建、通过单文件门禁、启动本地服务并返回 200 与最新 6,738,225 字节入口；测试后已用中断信号正常停止临时服务。
- 新增 `validate:project-paths` 与 `validate:startup-scripts` 并接入 `validate:all`：本轮扫描 301 个非依赖文件，旧项目路径引用为 0；两个 macOS 启动脚本均通过 zsh 语法检查。
- 重新主审并打包 W3–W12 十个实验资料包：W3–W5 增加可判定的 SQL/书面交付自检，W6 增加 JSON 根节点回归，W7 以 CSV 为 20×10 单一权威源并执行 20/20 场景，W9–W11 补齐质量、实验与评分校验；目录与 ZIP 内容逐文件一致，中文文件名均使用 UTF-8 标记。
- 新增 `validate:lab-downloads` 并接入 `build` 与 `validate:all`：验证根单文件 `./dist/labs/`、dist 入口 `./labs/`、十条权威 ZIP 路径、生产脚本引用和 public/dist 字节一致性；本地 HTTP 实际请求 10/10 返回 200 且 Content-Length 与源 ZIP 一致。最终全量路径扫描更新为 329 个非依赖文件、旧路径引用 0。
- 最新 375px 浏览器回归覆盖 Today、课程路线、W1D1 和 W1D2：均无页面级横向溢出，W1D2 不泄漏 W1D1 沙盒，控制台无 warning/error。
