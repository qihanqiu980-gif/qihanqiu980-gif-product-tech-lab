---
stage: 00
status: completed
completed_on: 2026-08-18
---
# 阶段 00：治理与课程生产流水线

## 目标

建立后续 Agent 可重复执行的课程生产系统，让每门课受依赖、内容质量、证据和工程门禁约束。

## 交付与验收

- 根 `AGENTS.md`：事实源、顺序、状态机、纪律和命令。
- `$product-learning-course-builder`：单 Day、阶段推进、证据和完成流程。
- Skill references：单 Day 完成定义与验证矩阵。
- 阶段索引及 01–08 Spec。
- Skill frontmatter/`agents/openai.yaml` 合法；唯一活动阶段为 01；文档引用的源码和 scripts 真实存在。
- 本阶段不修改课程源码、证据数据、W1D1 基准或构建产物。

## 2026-08-20 完成后治理补充：W1D1 正式页面

- 阶段状态保持 `completed`；本补充只同步已经实现并验证的正式页面契约，不重新开启阶段 00。
- 冻结参考与 Base64 运输层继续保持原样；正式 W1D1 唯一授权的可见差异，是删除顶部“桌面／手机／设计评审／视觉修正版”整行预览工具。
- 项目 `AGENTS.md` 与 `$product-learning-course-builder` 已记录：保留隐藏兼容按钮，工具容器强制不参与布局，bridge 在 `<= 860px` 使用原手机状态、其余使用原桌面状态。
- 不可变项仍为课程正文、左右导航、双时段信息、7 章路线、练习、复述、重置、证据接口与历史记录。
- 已验证 `npm run validate:w1d1` 20/20、`npm run validate:all`、`npm run build` 和离线单文件门禁；1440/1024/860/390/320px 无工具行、无横向溢出。2026-08-20 根 `index.html` SHA-256 为 `ddb821851681bcbe24b9ef541b5dc35febcf22c8a26bb95813ca1f136de265eb`。
