# 产品技术实验室阶段 Spec

Skill 负责“怎么生产”，本目录负责“本阶段交付什么、如何验收”。

## 当前状态

- 当前可用：W1D1、W1D2、W1D3、W1D4、W1D5、W3D1、W3D2、W3D3、W3D4、W3D5、W3D6、W4D1、W4D2、W11D1、W11D2、W11D3、W11D4、W11D5、W11D6、W12D1、W12D2、W12D3、W12D4、W12D5、W12D6（25/72）
- 当前活动：阶段 01 保持唯一 `active`；W1D5、W11D1 与 W11D4 已获用户亲自确认，W1D6 未制作。W3D1、W3D2、W3D3、W3D4、W3D5、W3D6、W4D1、W11D6、W12D1、W12D2、W12D3、W12D4、W12D5 与 W12D6 是用户另行授权的跨阶段单日插入；阶段 03、阶段 06 与阶段 07 仍为 `pending`。
- 最终发布：72/72 available 且 `npm run build:release` 通过

## 索引

| 阶段 | 范围 | 状态 | 交付主题 |
|---|---|---|---|
| 00 | 治理与生产流水线 | completed | AGENTS、Skill、Specs、状态与门禁 |
| 01 | W1D4–W1D6 | active | API/后端/数据库、Network 证据、综合诊断 |
| 02 | W2D1–W2D6 | pending | 环境、身份、日志、缓存、发布恢复、复测闭环 |
| 03 | W3D1–W5D6 | pending | 终端/SQLite/SQL、指标、JOIN 对账 |
| 04 | W6D1–W7D6 | pending | API 契约、字段边界、权限与异常验收 |
| 05 | W8D1–W9D6 | pending | Python、JSON/CSV、pandas 清洗 |
| 06 | W10D1–W11D6 | pending | 埋点/实验、AI 系统/RAG/工具/安全/评测 |
| 07 | W12D1–W12D6 | pending | 作品集、答辩与复盘 |
| 08 | 全局收口 | pending | 72/72、离线发布、全局回归 |

状态只用 `pending`、`active`、`blocked`、`completed`；同一时间只能一个 `active`。完成阶段后更新下一阶段和本页状态。

## 治理补充

- 2026-08-26：W4D2《聚合与分组入门》按跨阶段单日插入完成并进入 available。正式课程只首教输入行、WHERE、COUNT DISTINCT、SUM、AVG、GROUP BY 与 HAVING 的聚合观察，不提前教学真实生产结论、JOIN 对账或后续 SQL 综合任务；成果为 `aggregation-logic.md`／“聚合逻辑图”。W4D2 专项、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all`、`npm run build` 与 1440／1024／860／390／320px 运行时验收通过；最终 `index.html` 为 10,211,627 bytes，SHA-256 `00bfc1a8ef5a4932de23a073c7f747225180b66ccf18845aa5310c7092bbd2ac`。inventory 为 25/72；阶段 01 仍为唯一 `active`，阶段 03 仍为 `pending`。
- 2026-08-26：按用户确认只修改 W3D3 正式内容，不改变 Day01 外壳、背景、页面设计或其他课程正文。W3D3 内容版本更新为 `w3d3-basic-sql-queries-day01-v6`：入口更明确说明“今天学的是单表基础查询”，第 1 章先展示 orders 表字段与 8 行样本，补充 order_id/user_id/status/channel/paid_at 如何对应订单、用户、状态、渠道和支付时间；完整查询新增“拆开看概念”，把 FROM、WHERE、日期边界、SELECT、AS、ORDER BY、LIMIT 逐项绑定到实际行变化。第 5 章 6 道查询练习台去掉抽象 metric/conclusion 展示，改为“这一题先填什么”和“结果怎样变化”；第 7 章继续按 Q1–Q6 逐题生成 `basic-query-log.md`，字段清单仅作为写完后的自检。`validate:w3d3`、`validate:day01-framework`、`npm run validate:all`、`npm run build` 与 W3D3 运行时 1440／1024／860／390／320px 验收通过；最终 `index.html` 为 9,904,790 bytes，SHA-256 `88d5c4734e6834c612c4648fe77449e9bac383ea7e31f8e76d4d860997aefba3`。阶段 01 仍为唯一 `active`，阶段 03 仍为 `pending`。
- 2026-08-25：按用户确认只修订 W3D3 正式内容，不改变 Day01 外壳、背景、页面设计、路由注册或其他课程。W3D3 内容版本更新为 `w3d3-basic-sql-queries-day01-v4`，保留“先展示 orders 表、先跑完整查询、Q1–Q6 六道正向查询、常见错法集中到第 6 章”的主线，并进一步把第 7 章成果说明改为“每完成一题生成一条记录”，降低字段清单前置压力。`validate:w3d3`、`npm run validate:all`、`npm run build` 与 W3D3 运行时 1440／1024／860／390／320px 验收通过；最终 `index.html` 为 9,751,167 bytes，SHA-256 `ab1b5bd663ac49507e7ff85595dff6be30da32ebed71233f52c9a299e57f5d72`。inventory 仍为 22/72，W3D6 仍 missing，阶段 01 仍为唯一 `active`，阶段 03 仍为 `pending`。
- 2026-08-20：阶段 00 增补 W1D1 正式页面契约。冻结参考保持不变，正式页唯一授权可见差异为删除顶部预览工具行；隐藏兼容按钮负责原脚本初始化，设备模式随真实视口自动同步。阶段 00 仍为 `completed`，阶段 01 仍为唯一 `active`。
- 2026-08-20：后续日课改为 Day 01 两时段七章可见框架，12 个 DailyCourse 字段保留为数据与证据合同；已制作 Day 的路由不依赖前一天完成，可提前观看。每轮只改造一个 Day，完成自动验收后等待用户亲自确认。
- 2026-08-20：旧独立落地页方案已被推倒；W1D2 的 `day01-framework-w1d2` renderer 现复刻正式 W1D1 的 224px 全局学习栏、中间连续教材、244px sticky 七章目录与 860px 手机形态，只替换 URL／域名／DNS／服务器寻址内容。课程路线在应用工作台内切换正文，旧“返回课程路线＋30/45 分钟”工具栏已删除；学习／练习／复述分项状态、重置撤销、草稿和证据 bridge 均保留。
- 2026-08-20：1440／1024／860／390／320px 已自动验证三栏/手机比例、sticky 与点击定位、单一纵向滚动、无横向溢出、移动目录、分项高亮、重置撤销和刷新恢复；W1D1 五断点回归及冻结哈希通过。`npm run validate:all` 与 `npm run build` 通过；正式 `index.html` 为 7,239,780 bytes，SHA-256 `1adafdef2fe4b47ded552da9991016082c0c4d3b6fa197776c4fd08a84846ccf`，外部运行资源 0。Day 02 随后于 2026-08-21 获用户亲自确认。
- 2026-08-21：用户已亲自确认 Day 02。W1D3 旧通用日课方案已推倒，改为 `day01-framework-w1d3` 专属 renderer／bridge：正式工作台外壳、224px 左栏、244px sticky 七章目录、860px 手机形态与 Day 01 一致，只教授 HTTP Request／Response、Method、Header、Cookie、Query、Payload、Status、Timing 与 Size。
- 2026-08-21：W1D3 已在 1440／1024／860／390／320px 验证外壳几何、单一主滚动、无横向溢出、目录定位、移动目录、学习／练习／复述分项状态、重置撤销、刷新恢复、HTTP 教学模拟和应用内路线；W1D1／W1D2 回归及冻结哈希通过。`npm run validate:all` 与 `npm run build` 通过；正式 `index.html` 为 7,198,945 bytes，SHA-256 `1957422a1756a7f21e4025391c886ed4cd93ee0deb40698695199557790b34be`，外部运行资源 0。当前等待用户亲自确认 Day 03，确认前不开始 W1D4。
- 2026-08-21：用户已亲自确认 Day 03。W1D4 旧方案已推倒，改为 `day01-framework-w1d4` 专属 renderer／bridge；两时段七章只教 API 入口、请求处理器、后端业务服务、业务规则、数据库读写、持久化边界和 Response 组装。三条强制教学模拟覆盖合法写入、规则拒绝与数据库失败，成果为“请求处理链路说明”。
- 2026-08-21：W1D4 已在最终静态 `index.html` 的 1440／1024／860／390／320px 验证正式外壳、七章、sticky／移动目录、目录定位、键盘、可访问名称、单一主滚动、无横向溢出与应用内路线；学习／练习／复述分项状态、重置、刷新、草稿、追加式历史、证据边界和七概念 42 项复习排程通过。W1D1–W1D3 五视口及冻结哈希回归通过。`npm run validate:all` 与 `npm run build` 通过；库存为 4/72，W1D5+ 仍 missing。正式 `index.html` 为 7,341,827 bytes，SHA-256 `1c4d293e1e1acdb7e6a46c35915d31759c8ace1800d3981e23de77d211eca626`，外部运行资源 0。当前等待用户亲自确认 Day 04，确认前不开始 W1D5。
- 2026-08-21：用户已亲自确认 Day 04。W1D5 新增 `day01-framework-w1d5` 专属 renderer／bridge；两时段七章逐项首教请求条目、Name／URL、Method、Status、Type、Initiator、Headers、Payload、Response、Timing、Size、缓存线索与 Network 证据边界。教学观察器覆盖正常、4xx、5xx、慢 Timing、Size、Initiator 与缓存七类脱敏模拟，成果为“Network 观察记录”。库存由真实门禁更新为 5/72，W1D6 仍 missing；当前等待用户亲自确认 Day 05，确认前不得开始 W1D6。
- 2026-08-21：W1D5 最终专项门禁、`npm run validate:all` 与 `npm run build` 通过；1440／1024px 的 224px + 正文 + 244px 三栏合同、860／390／320px 手机合同、sticky／移动七章目录、单一 `.main-canvas` 纵向滚动面、唯一受控 Network 表格横向滚动、状态撤销、草稿落盘、深链和可访问名称均进入可重复源码门禁。本轮工具的 `file://` 安全协议拒绝打开用户批准参考与离线产物，因此没有将未执行的真实浏览器五视口交互写成“通过”。最终 `index.html` 为 7,477,010 bytes，SHA-256 `3781301f3d127c853259364998a2b6d37507426d0fba1d716ee969d98ea090ba`，外部运行资源 0。
- 2026-08-22：用户已亲自确认 W1D5，相关任务停止；阶段 01 继续为唯一 `active`，未制作 W1D6。按用户明确授权跨阶段只插入 W11D1，阶段 06 保持 `pending`。W11D1 以正式路线／Spec 的“AI 系统链”首教主题重做为 Day 01 两时段七章专属课程，RAG 与工具只作能力槽位定位，不提前教学后续机制；W11D2 当时仍为 `content-missing`。
- 2026-08-22：W11D1 在最终 `index.html` 的 1440／1024／860／390／320px 完成正式外壳几何、七章、sticky／移动目录、定位、键盘、21 个移动三态可访问名称、学习／练习／复述独立保存与撤销、实验／成果／记忆重置、刷新恢复、单一 `.main-canvas` 纵向滚动和无横向溢出验收；W1D1 同五档冻结参考、正式工作台导航与 15 个冻结哈希回归通过。全量门禁和构建通过，inventory 为 6/72；最终 `index.html` 为 7,605,243 bytes，SHA-256 `9c8d3c53a11ea76fad07dca06adaebcca203fc38816e0c09adf6fe8db597b225`，外部运行资源 0。用户随后已亲自确认 W11D1。
- 2026-08-22：用户依据正式路线纠正 W11D2：本日只完整首教“工具调用”，不把 RAG 并入同日主课。八个首次教学概念覆盖动作边界、工具目录／schema、调用提案、参数校验、身份权限、人工确认、执行器／副作用和结果回执／交付审计；六种教学模拟覆盖工具未注册、参数缺失、权限拒绝、确认缺失、执行失败／结果不确定和教学成功。成果仍为“AI 调用图”，正式 bridge 接回草稿、章节访问、追加式历史、v2 证据和 48 项六阶段复习。
- 2026-08-22：先前“双链同日”稿及其自动／浏览器结果已撤销，不能作为 W11D2 验收事实。工具调用单主题版本已通过 `validate:w11d2`、`validate:all`、最终 `build` 和构建后专项／inventory 复核；1440／1024／860／390／320px 的 W11D2 外壳、七章目录、sticky／移动目录、键盘、焦点、三态保存／撤销／刷新、六路径实验、成果草稿、追加式历史、复习排程、单一滚动与无横向溢出均通过，W1D1–W1D5／W11D1 同五视口及 20 个冻结哈希未漂移。课程路线和 `#/lesson/W11D2` 深链在 W11D1 未完成时仍可打开全文。inventory 为 7/72，W11D3 仍为 `content-missing` 且没有创建专属内容／视图／组件。最终 `index.html` 为 7,744,534 bytes，SHA-256 `21a29ad893ccb1a31c802ead5088402c4c6fe9e01856fde1a11aa1272df78653`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。冻结 W11D1 的 `nextLesson` 旧文案仍写“RAG 与工具调用”，本轮按范围要求只报告、不修改。当前等待用户亲自确认 W11D2，确认前不得开始 W11D3。
- 2026-08-23：用户另行授权只优化 W11D3。正式课程为“构建最小评测集”，成果为“20 条评测样本”；八个首次教学概念覆盖评测对象切片、评测维度与量表、case schema、预期行为判据、五类样本分层、对抗／隐私／拒答案例、覆盖矩阵与集合门禁。六条纯本地教学模拟分别拒绝缺 `expected_action`、模糊判据、refusal 配额缺口、重复输入、安全 hard gate 缺失，并接受五类各 4 条的合格集合。
- 2026-08-23：W11D3 已通过 `validate:w11d3`、`validate:all`、`npm run build`、构建后 inventory／冻结哈希和最终 `index.html` 五视口验收；课程路线与深链在 W11D2 零完成时仍可打开全文，W1D1–W1D5／W11D1／W11D2 同五视口均未漂移。inventory 为 8/72，W11D4–W11D6 仍 missing 且无专属内容／视图／组件。最终 `index.html` 为 7,883,726 bytes，SHA-256 `976b950a204b2aa5738b6a706da3a593907e830cee1151aa8b180a4c85c369f1`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。当前等待用户亲自确认 W11D3，确认前不得开始 W11D4。
- 2026-08-23：用户继续授权只处理 W11D4。正式课程为“幻觉从哪里产生”，成果为“失败分类表”；八个首次教学概念覆盖评测输出事实、知识缺失、召回缺口、资料冲突、生成过度推断、工具链失败、表达过度确定和失败分类表。召回缺口只按可见证据教学为 `source_exists=true` 且 `context_missing=true`，不展开 RAG 内部算法。
- 2026-08-23：W11D4 已通过 `validate:w11d4`、`validate:all`、`npm run build`、构建后 inventory／26 个冻结哈希和 localhost 运行时五视口验收；课程路线 `#/course` 显示 W1D1–W11D4（9/72），`#/lesson/W11D4` 深链不依赖 W11D3 完成状态。运行时发现并修复 `dist/index.html` 将 Vite module 脚本误写成 classic defer script 的 preview 空白问题：`dist/index.html` 保留 Vite 原始 module 标签，根目录 `index.html` 仍为内嵌离线单文件。最终 `index.html` 为 8,021,354 bytes，SHA-256 `46c66b71a15503f67726b891cb22f30871d585c5f4852df43ca4f11fee9d2d57`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。该时点 inventory 为 9/72，W11D5–W11D6 仍 missing 且无专属内容／视图／组件。
- 2026-08-23：用户已亲自确认 W11D4。W11D5 新增 `day01-framework-w11d5` 专属 renderer／bridge；正式课程为“成本、延迟与安全”，成果为“成本延迟安全风险建议表”。九个首次教学概念覆盖 token 与生成成本驱动、单位预算、延迟预算、关键路径、安全边界、安全拒答与安全完成、降级兜底、人工升级和风险建议表。六条纯本地教学模拟覆盖 token_budget_exceeded、tool_retry_cost、latency_budget_breach、timeout_degradation、safety_policy_block、human_escalation，均标注“教学模拟”并说明能／不能证明什么。`validate:w11d5`、`validate:all` 与 `npm run build` 通过；最终 `index.html` 为 8,150,284 bytes，SHA-256 `c35be60dbb41ac6ee000ae75681c9b91aa74c4f13a9383f53dfbdb6403d1d6c6`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。inventory 为 10/72，W11D6 仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件；当前等待用户亲自确认 W11D5，确认前不得开始 W11D6。
- 2026-08-23：用户继续授权只处理 W11D6。W11D6 新增 `day01-framework-w11d6` 专属 renderer／bridge；正式课程为“评测 AI 客服”，成果为“AI 客服综合评测报告”。八个首次教学概念覆盖评测报告范围、目标版本锁定、评测结果表、报告门槛、失败合成、风险合成、有限发布决策和报告证据边界。六段纯本地教学模拟覆盖 scope_version、result_summary、failure_analysis、risk_recommendation、launch_decision、follow_up_plan，均标注“教学模拟”并说明能／不能证明什么。
- 2026-08-23：W11D6 专项、`validate:all` 与 `npm run build` 通过；inventory 为 11/72，W12D1 仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件。最终 `index.html` 为 8,278,142 bytes，SHA-256 `1fc8e32fba2353cf90381ba3e63f2e2b3cd9a4a610f46c725e708855f71262cb`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。阶段 01 继续为唯一 `active`，阶段 06 继续为 `pending`；当前等待用户亲自确认 W11D6，确认前不得开始 W12D1。
- 2026-08-23：用户继续授权只处理 W12D1。W12D1 新增 `day01-framework-w12d1` 专属 renderer／bridge；正式课程为“定义值得解决的问题”，成果为“项目 Brief”。八个首次教学概念覆盖问题陈述、目标用户、利益相关方与影响、证据清单、缺失证据、成功指标、范围与非目标和 Brief 决策。六条纯本地教学模拟覆盖 problem-too-broad、user-missing、evidence-missing、metric-not-measurable、scope-creep、qualified-brief，均标注“教学模拟”并说明能／不能证明什么。`project-brief.md` 成果门禁要求 project_title、user_problem、target_user、stakeholders、evidence_used、missing_evidence、success_metric、scope、non_goals、assumptions、largest_unknown、next_step、evidence_limit 13 个字段齐全，且 next_step 只指向 W12D2 准备。
- 2026-08-23：W12D1 专项、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；localhost 运行时在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、课程路线入口、深链、学习／练习／复述状态、重置、刷新恢复、Brief 六路径观察器、项目 Brief 成果门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。最终 `index.html` 为 8,406,076 bytes，SHA-256 `1f87742e42b0f138594391201569db3f49664874b93a4883257d4fef5df81bc3`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。W12D1 收口时点 inventory 为 12/72，W12D2 当时仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件；W12D2 随后由用户另行授权。
- 2026-08-23：用户继续授权只处理 W12D2。W12D2 新增 `day01-framework-w12d2` 专属 renderer／bridge；正式课程为“系统与数据方案”，成果为“系统与数据方案”／`system-data-plan.md`。八个首次教学概念覆盖系统边界、用户旅程触点、数据对象、数据来源与责任、输入输出契约、状态生命周期、集成依赖和方案决策。六条纯本地教学模拟覆盖 boundary-missing、entity-missing、source-unknown、contract-missing、state-missing、qualified-plan，均标注“教学模拟”并说明能／不能证明什么。`system-data-plan.md` 成果门禁要求 brief_ref、system_goal、system_boundary、user_touchpoints、data_entities、data_sources、input_output_contracts、state_lifecycle、dependencies、privacy_access_boundary、validation_questions、non_goals、evidence_limit 13 个字段齐全，且 validation_questions 只指向 W12D3。
- 2026-08-23：W12D2 专项、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、课程路线入口、深链、学习／练习／复述状态、重置、刷新恢复、系统与数据方案六路径观察器、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。最终 `index.html` 为 8,537,924 bytes，SHA-256 `8faf71f7d08c344a91ef28072634219ff3905fd43b760d34b88f785be8bd2443`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。inventory 为 13/72，W12D3–W12D6 仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件。阶段 01 继续为唯一 `active`，阶段 06 与阶段 07 继续为 `pending`；当前等待用户亲自确认 W12D2，确认前不得开始 W12D3。
- 2026-08-25：用户继续授权只处理 W12D3。W12D3 新增 `day01-framework-w12d3` 专属 renderer／bridge；正式课程为“最大未知的可运行验证日志”，成果为“可运行验证日志”／`validation-log.csv`。八个首次教学概念覆盖最大未知、验证问题、可运行检查、测试夹具、通过/失败标准、运行日志、观察边界和验证决策。六条纯本地教学模拟覆盖 unknown-too-broad、question-missing、fixture-missing、criterion-missing、log-incomplete、qualified-validation，均标注“教学模拟”并说明能／不能证明什么。`validation-log.csv` 成果门禁要求 validation_id、brief_ref、unknown_to_test、validation_question、fixture_used、run_steps、expected_result、observed_result、pass_fail、evidence_collected、decision、cannot_prove、next_action 13 个字段齐全，且 next_action 只指向 W12D4 风险登记或修订验证。
- 2026-08-25：W12D3 专项、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、课程路线入口、深链、学习／练习／复述状态、重置、刷新恢复、可运行验证日志六路径观察器、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。最终 `index.html` 为 8,667,675 bytes，SHA-256 `3500b5eb3d4980830a388c698d65e9a29393197efb4036c4b29331eb99a52a63`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。inventory 为 14/72，W12D4–W12D6 仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件。阶段 01 继续为唯一 `active`，阶段 06 与阶段 07 继续为 `pending`；当前等待用户亲自确认 W12D3，确认前不得开始 W12D4。
- 2026-08-25：用户继续授权只处理 W12D4。W12D4 新增 `day01-framework-w12d4` 专属 renderer／bridge；正式课程为“风险登记册编写”，成果为“风险登记册”／`risk-register.csv`。八个首次教学概念覆盖风险来源、风险陈述、概率、影响、预防动作、监控信号、触发条件与兜底方案、责任人。六条纯本地教学模拟覆盖 risk-too-vague、probability-missing、impact-missing、prevention-missing、trigger-fallback-missing、qualified-risk-register，均标注“教学模拟”并说明能／不能证明什么。`risk-register.csv` 成果门禁要求 risk_id、evidence_ref、risk_statement、probability、impact、prevention_action、monitoring_signal、trigger_condition、fallback_plan、owner、review_cadence、status、evidence_limit 13 个字段齐全，status 只进入待评审/监控状态，不提前完成 W12D5 评审或 W12D6 作品集。
- 2026-08-25：W12D4 专项、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、课程路线入口、深链、学习／练习／复述状态、重置、刷新恢复、风险登记册六路径观察器、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。最终 `index.html` 为 8,796,402 bytes，SHA-256 `ba70ce3d394624a3023d1d89a2a18f032f0abb3e73ff1edbe14115abad623d4d`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。inventory 为 15/72，W12D5–W12D6 仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件。阶段 01 继续为唯一 `active`，阶段 06 与阶段 07 继续为 `pending`；当前等待用户亲自确认 W12D4，确认前不得开始 W12D5。
- 2026-08-25：用户继续授权只处理 W12D5。W12D5 新增 `day01-framework-w12d5` 专属 renderer／bridge；正式课程为“跨职能评审与修订”，成果为“评审纪要”／`review-notes.md`。八个首次教学概念覆盖评审角色、角色化追问、证据回答、缺口反馈、有限评审决策、修订动作、owner 确认和变更记录。六条纯本地教学模拟覆盖 role-missing、question-vague、evidence-missing、decision-missing、revision-owner-missing、qualified-review-notes，均标注“教学模拟”并说明能／不能证明什么。`review-notes.md` 成果门禁要求 review_id、artifact_ref、review_role、question、evidence_cited、answer_summary、gap_or_risk、decision、revision_action、owner、due_date、status、evidence_limit 13 个字段齐全，status 只进入 draft/pending_owner_confirmation/revising/ready_for_w12d6 等有限状态，不提前完成 W12D6 作品集、真实评审批准、owner 承诺或答辩通过。
- 2026-08-25：W12D5 专项、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、课程路线入口、深链、学习／练习／复述状态、重置、刷新恢复、评审纪要六路径观察器、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。最终 `index.html` 为 8,926,300 bytes，SHA-256 `6837290dc19dfbc6f8ea5d573e5e638f99636014939cd0b00c3867a44a5b9506`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。inventory 为 16/72，W12D6 仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件。阶段 01 继续为唯一 `active`，阶段 06 与阶段 07 继续为 `pending`；当前等待用户亲自确认 W12D5，确认前不得开始 W12D6。
- 2026-08-25：用户继续授权只处理 W12D6。W12D6 新增 `day01-framework-w12d6` 专属 renderer／bridge；正式课程为“作品集装配、10 分钟答辩和复盘”，成果为“完整作品集包”／`portfolio-checklist.md`。八个首次教学概念覆盖成果索引、证据线、作品集叙事、10 分钟答辩、演示边界、评审追问预案、复盘洞察和脱敏与证据边界。六条纯本地教学模拟覆盖 artifact-missing、evidence-thread-broken、pitch-too-long、demo-boundary-missing、qna-unprepared、qualified-portfolio-package，均标注“教学模拟”并说明能／不能证明什么。`portfolio-checklist.md` 成果门禁要求 portfolio_id、artifact_index、problem_story、solution_summary、evidence_thread、risk_review_summary、revision_log、ten_minute_pitch、demo_boundary、reviewer_qna、retrospective_insight、redaction_boundary、evidence_limit 13 个字段齐全；W12D6 是 12 周核心课程终点，不定义 `nextLesson`，不伪造 W13D1、W12D7、真实上线、真实业务价值、真实答辩通过、招聘结果或真实跨职能批准。
- 2026-08-25：W12D6 专项、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、课程路线入口、深链、学习／练习／复述状态、重置、刷新恢复、作品集装配六路径观察器、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。最终 `index.html` 为 9,059,565 bytes，SHA-256 `c354e3f92f1df9d3e67cc5438444cbb830564e0ba1cf9ecd1449b4b906bbe853`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。inventory 为 17/72，缺失范围为 W1D6、W2D1–W10D6；未创建 W13D1 或 W12D7。阶段 01 继续为唯一 `active`，阶段 06 与阶段 07 继续为 `pending`；当前等待用户亲自确认 W12D6，确认前不得开始任何后续课程。
- 2026-08-25：W3D1《打开 SQLite 实验环境》按跨阶段单日插入完成，成果为 `sql-lab-runlog.md`／“SQL 实验环境记录”。本日只首教终端、当前目录、路径、安全副本、SQLite 数据库文件、sqlite3 会话、setup.sql 和表/行/列最小预览，不提前教学 SELECT 执行顺序、筛选、排序、聚合、GROUP BY 或 JOIN。W3D1 专项、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、课程路线入口、深链、学习／练习／复述状态、重置、刷新恢复、SQLite 环境六路径观察器、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。最终 `index.html` 为 9,187,930 bytes，SHA-256 `d66e7b132e228130bb3b980cafeb9bf7eea2a7cb9c174d06f652f212f19a0c1a`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。inventory 为 18/72，W3D2 仍 missing／missingContent／missingImplementation，未创建 W3D2 或后续课程专属文件。阶段 01 继续为唯一 `active`，阶段 03、阶段 06 与阶段 07 继续为 `pending`；当前等待用户亲自确认 W3D1，确认前不得开始 W3D2。
- 2026-08-25：用户继续授权只处理 W3D2。W3D2 新增 `day01-framework-w3d2` 专属 renderer／bridge；正式课程为“SELECT 执行逻辑”，成果为“SQL 执行顺序图”／`sql-execution-order.md`。八个首次教学概念覆盖 SQL 语句、SELECT 子句、FROM 子句、WHERE 门、ORDER BY 阶段、LIMIT 阶段、执行顺序和结果边界。六条纯本地教学模拟覆盖 missing-from、where-before-source、select-column-mismatch、order-without-rule、limit-overclaim、qualified-select-order，只教学 FROM → WHERE → SELECT → ORDER BY → LIMIT 的查询执行观察，不提前完成 W3D3 基础查询集、聚合、GROUP BY、JOIN 或指标。`validate:w3d2`、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、学习／练习／复述状态、重置、刷新恢复、SELECT 执行顺序六路径观察器、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。课程路线 `#/course` 通过已开放日课入口，在 W3D1 无完成记录时仍可打开 W3D2。该时点最终 `index.html` 为 9,319,032 bytes，SHA-256 `a9839292e5d247e2f9a28f7fb9fba8899b68a8ae1dd50ba7e8e60c5f6d6a78fa`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。该时点 inventory 为 19/72，W3D3 尚未制作；后续 W3D3 已在下一条记录中完成。
- 2026-08-25：用户继续授权只处理 W3D3。W3D3 新增并于本轮修订 `day01-framework-w3d3` 专属 renderer／bridge；正式课程为“用户与订单基础查询”，成果为“6 道基础查询”／`basic-sql-queries.sql` 与 `basic-query-log.md`。八个首次教学概念覆盖查询问题、WHERE 条件、比较符、文本值筛选、日期范围边界、排序方向、列别名和 DISTINCT 结果去重。正式主线先展示 `orders` 表字段和样本行，再用一条完整查询拆概念，并以 Q1–Q6 六道正向查询覆盖 `q1-paid-orders`、`q2-app-channel-orders`、`q3-orders-on-20260831`、`q4-recent-paid-orders`、`q5-paid-time-alias`、`q6-distinct-channels`；每题都有业务问题、SQL、预期留下哪些行、实际看到什么、能证明什么、不能证明什么。常见错法集中放入第 6 章，不再作为第 5 章主线。`validate:w3d3`、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、学习／练习／复述状态、重置、刷新恢复、6 道查询练习台、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。课程路线 `#/course` 通过已开放日课入口，在 W3D2 无完成记录时仍可打开 W3D3。最终 `index.html` 为 9,460,083 bytes，SHA-256 `220e6f57526f5b30c69e3dec7752c989fbf362f8cbc62c6e69fe823b9b5f9db7`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。该时点 inventory 为 20/72，W3D4 当时仍 missing／missingContent／missingImplementation，未创建 W3D4 或后续课程专属文件；后续 W3D4 已在下一条记录中完成。
- 2026-08-25：用户继续授权只处理 W3D4。W3D4 新增 `day01-framework-w3d4` 专属 renderer／bridge；正式课程为“查询为何不可信”，成果为“数据质量检查表”／`data-quality-checklist.md`。八个首次教学概念覆盖数据质量问题、NULL 值、重复行、状态覆盖、测试数据标记、时间边界风险、金额异常和质量证据边界。正式主线先展示 `orders` 表中的具体风险行 O1008、O1009/TEST1、O1014、O1016、O1017、O1018，再以 C1–C6 六条质量检查覆盖 `null-user-id`、`null-paid-at`、`duplicate-order-id`、`test-order-included`、`zero-or-null-amount`、`boundary-cross-day`；每条都有业务问题、SQL、预期风险、实际看到什么、能证明什么、不能证明什么。`validate:w3d4`、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、目录定位、学习／练习／复述状态、重置、刷新恢复、6 条数据质量检查台、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。课程路线 `#/course` 通过已开放日课入口，在 W3D3 无完成记录时仍可打开 W3D4。最终 `index.html` 为 9,601,479 bytes，SHA-256 `bdc84271f6d122becc2c1445f7691334ba21babe2e1c9c89076252003fcb8d65`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。该时点 inventory 为 21/72，W3D5 仍 missing／missingContent／missingImplementation，未创建 W3D5 或后续课程专属文件；W3D5 已在下一条记录中完成。
- 2026-08-25：用户继续授权只处理 W3D5。W3D5 新增 `day01-framework-w3d5` 专属 renderer／bridge；正式课程为“向数据研发确认口径”，成果为“取数需求单”／`data-request.md`。八个首次教学概念覆盖取数需求目的、业务问题范围、字段与来源假设、筛选规则契约、时间窗口契约、排除与标注规则、期望输出形状、复核问题与证据边界。正式主线把 W3D3 的基础查询和 W3D4 的质量风险转成数据研发可复核口径，六条确认路径覆盖 `scope-missing`、`field-source-missing`、`filter-rule-missing`、`time-window-ambiguous`、`quality-exclusion-missing`、`qualified-data-request`；每条都有业务问题、修正写法、预期补齐什么、实际形成什么、能证明什么、不能证明什么。W3D5 只完成 `data-request.md`，不提前教学聚合、GROUP BY、JOIN、指标计算或 W3D6 业务查询集。`validate:w3d5`、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；headless Chrome 在 1440／1024／860／390／320px 验证正式工作台外壳、七章目录、sticky／移动目录、课程路线入口、深链、学习／练习／复述状态、重置、6 条取数需求确认台、成果字段门禁、单一 `.main-canvas` 主纵向滚动和无页面级横向溢出。最终 `index.html` 为 9,750,950 bytes，SHA-256 `90d49088e6d9e1b8a202c3f168ae54d0eb2ac36c29d9dae290a4a6ce388988b8`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。inventory 为 22/72，W3D6 仍 missing／missingContent／missingImplementation，未创建 W3D6 或后续课程专属文件。阶段 01 继续为唯一 `active`，阶段 03、阶段 06 与阶段 07 继续为 `pending`。
