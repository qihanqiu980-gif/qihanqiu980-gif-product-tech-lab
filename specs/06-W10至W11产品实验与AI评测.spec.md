---
stage: 06
status: pending
scope: W10D1-W11D6
depends_on: stage-05 completed
---
# 阶段 06：产品实验与 AI 评测

## 功能

W10 分日首教事件/属性/用户/会话、漏斗/留存/cohort、实验单位、入组、曝光、随机化、SRM、护栏、周期和停止规则；使用 `W10-实验设计` 解释 45:55 分流和覆盖缺口，形成埋点/指标/实验决策记录。

W11 当前正式逐日路线按系统链 → 工具调用 → 最小评测集 → 失败分类 → 成本/延迟/安全 → 综合评测推进；W11D2 只教授工具调用的合同、提案、参数、权限、确认、执行、副作用与回执，真实动作只做纯本地教学模拟。RAG 的首次教学排期不由本次 W11D2 单日任务猜测或扩写；阶段 06 正式启动时必须先依据届时路线单独锁定。使用 `W11-AI评测`，学习者新增独立样本，成果为有门槛、失败分类、风险和建议的评测报告。

## W11D1–W11D5 授权跨阶段单日插入（2026-08-22 至 2026-08-23）

- 用户先后明确授权只重做 W11D1、W11D2、W11D3、W11D4、W11D5；这些单日插入不代表阶段 06 启动。本 Spec 的 `status` 继续为 `pending`，阶段 01 仍是唯一 `active`。
- W11D1 的正式主题为“AI 功能不是一个 Prompt／AI 系统链路”：请求 → 上下文 → 模型 → 能力槽位 → 业务规则／编排 → 输出交付／人工兜底 → 运行观察／审计。RAG 与工具只定位为彼此独立的能力槽位，不提前首教检索内部机制、工具权限与真实动作、失败分类、安全或评测。
- W11D1 以 Day 01 两时段七章外壳独立实现；前置不足只显示补学提醒，不依赖 W10 或上一阶段完成状态锁定全文。草稿、章节访问、追加式历史、v2 证据账本与 D1／D3／D7／D14／D30／D60 复习全部经正式 adapter／bridge 保存。
- 本地 AI 系统链观察器覆盖完整链、上下文缺失、能力不可用、规则阻断四种变式，并明确不调用真实模型、RAG、工具、账号、Token、公司数据或生产系统。成果为“AI 系统链路说明”。
- W11D1 专项与全量门禁、最终静态构建、1440／1024／860／390／320px 浏览器验收及 W1D1／冻结哈希回归通过；用户随后已亲自确认 W11D1。
- 用户依据正式路线纠正：W11D2 的正式主题只为“工具调用”，成果为“AI 调用图”。调用链为 action intent → tool registry／schema → proposal／arguments → validation → authorization → confirmation → executor／side effect → receipt／delivery／audit；W11D3 仍只负责“构建最小评测集”。
- W11D2 使用六种纯本地教学模拟：工具未注册、参数缺失、权限拒绝、缺少人工确认、执行失败／结果不确定、工具教学成功；不调用真实模型、工具、账号、Token、公司数据或生产系统。执行前失败必须是 `executorCalled=false`；超时未知路径为 `executorCalled=true` 且保留 callId，不能盲目宣称业务未变化；成功只返回本地教学回执。
- 先前“RAG 与工具调用双链同日”稿及其 7,739,489-byte 构建、哈希和浏览器结果均已撤销，不得作为本版本验收事实。当前单主题实现有八个首次教学概念和 48 项六阶段复习；W11D2 专项、`validate:all`、最终构建、1440／1024／860／390／320px、课程路线／深链、W1D1–W1D5／W11D1 回归及 20 个冻结哈希均通过。最终 `index.html` 为 7,744,534 bytes，SHA-256 `21a29ad893ccb1a31c802ead5088402c4c6fe9e01856fde1a11aa1272df78653`，内嵌样式 1、内嵌脚本 2、内嵌 WOFF2 101、外部运行资源 0。这是 W11D2 收口时点的 7/72 冻结基线；W11D3 随后由用户另行授权。
- 冻结 W11D1 的 `nextLesson` 仍含“RAG 与工具调用”旧桥接文案，与用户最新正式路线冲突；本轮遵守“不修改 W11D1”要求，只记录问题，不改冻结源码或验收状态。
- W11D3 只首教“构建最小评测集”，成果为“20 条评测样本”；不运行真实模型，不提前诊断 W11D4 的失败根因，也不设计 W11D5 的成本／延迟／安全方案。八个概念依次覆盖评测对象切片、评测维度与量表、case schema、预期行为判据、五类样本分层、对抗／隐私／拒答案例、覆盖矩阵与集合门禁。
- W11D3 的专属本地教学观察器覆盖缺 `expected_action`、判据只写“回答合理”、refusal 类为空、重复输入只改 `case_id`、privacy 的 `hard_gate=false` 和五类各 4 条合格集合六条路径。集合 valid 只证明 20 行 JSONL、五类配额、唯一性、字段、正负断言、安全 hard gate、工具阶段覆盖与同一 target version 通过合同，不证明模型表现、线上代表性或生产安全。
- W11D3 renderer 为 `day01-framework-w11d3`，实验为 `minimum-eval-set-gate-observer-day01-v1`，证据为 `w11d3-evidence-v2`；正式 bridge 继续保存草稿、章节访问、追加式历史、待审核成果和八概念 48 项六阶段复习，不建立平行状态源。30 分钟路径为 3 题／3 份实验记录／160 字贡献门槛，45 分钟路径为 4 题／4 份记录／260 字门槛，两者保留完整首教和 6 个实验步骤。
- W11D3 专项、全量门禁、最终构建、构建后 inventory／23 个冻结哈希与 `index.html` 的 1440／1024／860／390／320px 验收均通过；路线与 `#/lesson/W11D3` 深链不依赖 W11D2 完成状态，W1D1–W1D5／W11D1／W11D2 同五视口未漂移。最终 `index.html` 为 7,883,726 bytes，SHA-256 `976b950a204b2aa5738b6a706da3a593907e830cee1151aa8b180a4c85c369f1`，内嵌样式 1、内嵌脚本 2、内嵌 WOFF2 101、外部运行资源 0。inventory 为 8/72；W11D4–W11D6 仍 missing 且无专属内容／视图／组件，等待用户亲自确认 W11D3 前不得开始 W11D4。
- W11D4 只首教“幻觉从哪里产生”，成果为“失败分类表”；不运行真实模型，不证明生产根因，不提前设计 W11D5 的成本、延迟、安全或降级方案。八个概念依次覆盖评测输出事实、知识缺失、召回缺口、资料冲突、生成过度推断、工具链失败、表达过度确定和失败分类表；“召回缺口”只按可见证据教学为资料存在但本次上下文缺失，不展开 RAG 内部检索算法。
- W11D4 的专属本地教学观察器覆盖 knowledge_gap、retrieval_gap、source_conflict、generation_overreach、tool_chain、expression_overcertainty 六条路径。失败分类表门禁要求恰好 6 行 JSONL、六类各一行、唯一 `case_id`、完整 `expected_behavior`／`observed_output`／`evidence_seen`／`first_failed_stage`／`product_action`／`evidence_limit`；工具链行必须含 callId 或 receipt，召回行必须同时含 `source_exists=true` 与 `context_missing=true`，每行均标注教学模拟且不得声称真实生产根因已证明。
- W11D4 renderer 为 `day01-framework-w11d4`，实验为 `failure-classification-gate-observer-day01-v1`，证据为 `w11d4-evidence-v2`；正式 bridge 继续保存草稿、章节访问、追加式历史、待审核成果和八概念 48 项六阶段复习，不建立平行状态源。前置不足只提示补学，路线和 `#/lesson/W11D4` 深链不依赖 W11D3 完成状态。
- W11D4 专项、全量门禁、最终构建、构建后 inventory／26 个冻结哈希与 localhost 运行时 1440／1024／860／390／320px 验收均通过；路线 `#/course` 显示 W1D1–W11D4（9/72），深链可打开全文，该时点 W11D5–W11D6 仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件。运行时修复 `dist/index.html` preview module 脚本被误写成 classic script 的空白问题，根目录离线 `index.html` 资源结构不变。最终 `index.html` 为 8,021,354 bytes，SHA-256 `46c66b71a15503f67726b891cb22f30871d585c5f4852df43ca4f11fee9d2d57`，内嵌样式 1、内嵌脚本 2、内嵌 WOFF2 101、外部运行资源 0。
- 用户已亲自确认 W11D4 后继续授权只处理 W11D5。W11D5 只首教“成本、延迟与安全”，成果为“成本延迟安全风险建议表”；不运行真实模型，不证明真实账单、生产 P95 或正式合规，不提前完成 W11D6 的综合评测报告。九个概念依次覆盖 token 与生成成本驱动、单位预算、延迟预算、关键路径、安全边界、安全拒答与安全完成、降级兜底、人工升级和风险建议表。
- W11D5 的专属本地教学观察器覆盖 token_budget_exceeded、tool_retry_cost、latency_budget_breach、timeout_degradation、safety_policy_block、human_escalation 六条路径。成果门禁要求恰好 6 行 JSONL，逐行包含 `case_id`、`cost_driver`、`latency_driver`、`safety_risk`、`decision`、`product_action`、`evidence_limit`；成本、延迟、安全各至少 2 行，至少 1 行人工升级为 `decision=escalate`，每行证据边界必须标注教学模拟。
- W11D5 renderer 为 `day01-framework-w11d5`，实验为 `cost-latency-safety-risk-observer-day01-v1`，证据为 `w11d5-evidence-v2`；正式 bridge 继续保存草稿、章节访问、追加式历史、待审核成果和九概念 54 项六阶段复习，不建立平行状态源。前置不足只提示补学，路线和 `#/lesson/W11D5` 深链不依赖 W11D4 完成状态。
- W11D5 专项、全量门禁和最终构建通过；`npm run audit:course-inventory` 显示 W1D1–W1D5、W11D1–W11D5 共 10/72 available，W11D6 仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件。最终 `index.html` 为 8,150,284 bytes，SHA-256 `c35be60dbb41ac6ee000ae75681c9b91aa74c4f13a9383f53dfbdb6403d1d6c6`，内嵌样式 1、内嵌脚本 2、内嵌 WOFF2 101、外部运行资源 0。当前等待用户亲自确认 W11D5，确认前不得开始 W11D6。
- 用户继续授权只处理 W11D6。W11D6 只首教“评测 AI 客服”，成果为“AI 客服综合评测报告”；不运行真实模型，不证明真实生产表现、生产账单、生产 P95、正式合规或上线审批，不提前创建 W12D1 作品集材料。八个概念依次覆盖评测报告范围、目标版本锁定、评测结果表、报告门槛、失败合成、风险合成、有限发布决策和报告证据边界。
- W11D6 的专属本地教学观察器覆盖 scope_version、result_summary、failure_analysis、risk_recommendation、launch_decision、follow_up_plan 六段路径。成果门禁要求恰好 6 行 JSONL，逐行包含 `section_id`、`report_part`、`evidence_used`、`conclusion`、`recommendation`、`evidence_limit`；六段各一行，发布决策只能是 `allow_pilot`、`fix_before_pilot`、`block` 或 `manual_review`，每行证据边界必须标注教学模拟并写清不能证明什么。
- W11D6 renderer 为 `day01-framework-w11d6`，实验为 `ai-service-eval-report-observer-day01-v1`，证据为 `w11d6-evidence-v2`；正式 bridge 继续保存草稿、章节访问、追加式历史、待审核成果和八概念 48 项六阶段复习，不建立平行状态源。前置不足只提示补学，路线和 `#/lesson/W11D6` 深链不依赖 W11D5 完成状态。
- W11D6 专项、全量门禁和最终构建通过；`npm run audit:course-inventory` 显示 W1D1–W1D5、W11D1–W11D6 共 11/72 available，W12D1 仍 missing／missingContent／missingImplementation 且无专属内容／视图／组件。最终 `index.html` 为 8,278,142 bytes，SHA-256 `1fc8e32fba2353cf90381ba3e63f2e2b3cd9a4a610f46c725e708855f71262cb`，内嵌样式 1、内嵌脚本 2、内嵌 WOFF2 101、外部运行资源 0。当前等待用户亲自确认 W11D6，确认前不得开始 W12D1。

## 验收

W10D1–W11D6 全部 available，累计 66；实验概念先于模拟，空评分表被拒绝，关键安全失败可阻断，结果可追溯、可审核并说明不确定性；全量门禁、构建和安全回归通过。
