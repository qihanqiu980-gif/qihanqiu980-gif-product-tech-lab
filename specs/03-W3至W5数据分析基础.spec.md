---
stage: 03
status: pending
scope: W3D1-W5D6
depends_on: stage-02 completed
---
# 阶段 03：数据分析基础

## 功能

W3 先教终端、路径、副本/只读安全、SQLite，再按日覆盖表/行/列、筛选、排序、聚合、分组和 SQL 复现；使用 `public/labs/W3-SQL基础`，每 Day 仍有预测、操作、解释和变式。

W4 坚持先分子/分母/去重/时间/排除规则再计算，使用 `W4-指标口径` 完成指标口径表的差稿修订。W5 首教主键/外键/匹配、一对多放大、缺失/重复，使用 `W5-JOIN对账` 形成可审核对账报告。

## 验收

W3D1–W5D6 全部 available，累计 30；SQL/周包自检、实验下载、路径安全、指标先定义后计算、JOIN 放大与未匹配检测、成果证据边界、`validate:lab-packages`/`validate:lab-downloads`/`validate:all`/`build` 均通过。

## 当前进展

- 2026-08-26：W4D2《聚合与分组入门》按跨阶段单日插入完成，成果为 `aggregation-logic.md`／“聚合逻辑图”。本日只首教输入行、WHERE、COUNT DISTINCT、SUM、AVG、GROUP BY 与 HAVING 的聚合观察，不提前教学真实生产结论、JOIN 对账或后续 SQL 综合任务。W4D2 专项、Day01 框架、TypeScript、实现注册、路由、inventory、`npm run validate:all` 与 `npm run build` 通过；`#/lesson/W4D2` 与 `#/course` 在 1440／1024／860／390／320px 的 runtime 与 rebuilt-offline 验收通过。最终 `index.html` 为 10,211,627 bytes，SHA-256 `00bfc1a8ef5a4932de23a073c7f747225180b66ccf18845aa5310c7092bbd2ac`，含 1 个内嵌样式、2 个内嵌脚本、101 个内嵌 WOFF2，外部运行资源 0。inventory 为 25/72，阶段 03 仍因 W4D3-W5D6 未完成保持 `pending`。
- 2026-08-26：按目标继续完成 W3D6，当前只补齐 W3D6 完成记录，不创建或修改 W4D1，也不改变 Day01 外壳、背景、页面设计或其他课程正文。W3D6 内容版本为 `w3d6-business-query-set-day01-v1`，正式主题为“回答真实业务问题”，成果为 `business-query-set.sql` 与 `business-query-log.md`／“12 道业务查询集”。本日把 W3D3 基础查询、W3D4 质量风险和 W3D5 取数需求收束为 BQ01-BQ12 十二条单表业务查询，覆盖 `bq01-paid-app-orders`、`bq02-20260831-paid-orders`、`bq03-boundary-after-midnight`、`bq04-recent-paid-orders`、`bq05-app-nonpaid-orders`、`bq06-distinct-channel-values`、`bq07-test-order-risk`、`bq08-duplicate-order-risk`、`bq09-amount-review-risk`、`bq10-paid-time-alias`、`bq11-null-user-risk`、`bq12-review-ready-app-paid`；每条记录要求 query_id、business_question、request_ref、base_table、sql_text、expected_rows、observed_result、quality_flags、can_prove、cannot_prove、handoff_note、next_review_question。边界限定为本地 `orders` 单表明细查询、质量标注和交接说明，不提前教学或宣称聚合、GROUP BY、JOIN、指标、真实生产结论或 W4D1 已完成。已通过 `npm run validate:w3d6`、`npm run validate:day01-framework`、`npm run validate:course-catalog`、`npm run validate:course-implementations`、`npm run validate:day-routes`、`npm run validate:evidence`、`npm run validate:all`、`npm run build`；W3D6 运行时 `1440／1024／860／390／320px` 验证通过，7 章目录、12 条查询覆盖、课程路线入口、深链、课程导航活动态和无页面级横向溢出均通过。最终 `index.html` 为 9,904,790 bytes，SHA-256 `88d5c4734e6834c612c4648fe77449e9bac383ea7e31f8e76d4d860997aefba3`。当前 inventory 为 23/72，W4D1 仍为 missing／missingContent／missingImplementation，阶段 03 因 W4D1-W5D6 未完成继续保持 `pending`。
- 2026-08-26：按用户确认只修改 W3D3 正式内容，不改变 Day01 外壳、背景、页面设计或其他课程正文。W3D3 内容版本更新为 `w3d3-basic-sql-queries-day01-v6`：第 1 章进一步把“今天学的是单表基础查询”放在入口，先展示 orders 表字段和 8 行样本，并补充 order_id/user_id/status/channel/paid_at 如何对应订单、用户、状态、渠道和支付时间；完整查询新增“拆开看概念”，把 FROM、WHERE、日期边界、SELECT、AS、ORDER BY、LIMIT 逐项绑定到实际行变化。第 5 章保留 Q1–Q6 六道正向查询练习台，并把抽象 metric/conclusion 展示改为“这一题先填什么”和“结果怎样变化”；第 6 章集中讲日期边界、文本引号、未排序说最新和 DISTINCT 越界；第 7 章把完整字段表折叠为写完一题后的自检参考，主线仍为逐题生成 `basic-query-log.md`。`validate:w3d3`、`validate:day01-framework`、`npm run validate:all`、`npm run build` 与 W3D3 运行时 1440／1024／860／390／320px 验收通过；最终 `index.html` 为 9,904,790 bytes，SHA-256 `88d5c4734e6834c612c4648fe77449e9bac383ea7e31f8e76d4d860997aefba3`。阶段 03 仍为 `pending`。
- 2026-08-25：W3D3 正式内容按用户确认完成一次窄范围修订，未改变 Day01 外壳、背景、页面设计、路由注册或其他课程。内容版本为 `w3d3-basic-sql-queries-day01-v4`；第 7 章成果说明从前置字段清单改为按 Q1–Q6 每题逐步生成 `basic-query-log.md` 记录，并保留 `basic-sql-queries.sql`、orders 表样本入口、完整查询示范、6 道正向查询练习台和第 6 章集中纠错。`validate:w3d3`、`npm run validate:all`、`npm run build` 与 W3D3 运行时 1440／1024／860／390／320px 验收通过。inventory 仍为 22/72，W3D6 仍 missing／missingContent／missingImplementation，阶段 03 仍为 `pending`。
- 2026-08-25：W3D1 作为用户另行授权的跨阶段单日插入已完成并进入 available，但阶段 03 仍为 `pending`，不代表 W3D2–W5D6 启动或阶段完成。
- W3D1 正式主题为“打开 SQLite 实验环境”，成果为 `sql-lab-runlog.md`／“SQL 实验环境记录”；首教边界限定为终端、当前目录、路径、安全副本、SQLite 数据库文件、sqlite3 会话、setup.sql 和表/行/列最小预览。
- 2026-08-25：W3D2 作为用户另行授权的跨阶段单日插入已完成并进入 available，但阶段 03 仍为 `pending`，不代表 W3D3–W5D6 启动或阶段完成。
- W3D2 正式主题为“SELECT 执行逻辑”，成果为 `sql-execution-order.md`／“SQL 执行顺序图”；首教边界限定为 SQL 语句、SELECT 子句、FROM 子句、WHERE 门、ORDER BY 阶段、LIMIT 阶段、执行顺序和结果边界。
- W3D2 只教学 FROM → WHERE → SELECT → ORDER BY → LIMIT 的执行观察、证据边界和成果图，不提前完成 W3D3 基础查询集、聚合、GROUP BY、JOIN、指标或业务查询答案；W3D3 已在后续授权中单独完成，见下方记录。
- 2026-08-25：W3D3 作为用户另行授权的跨阶段单日插入已完成并进入 available，但阶段 03 仍为 `pending`，不代表 W3D4–W5D6 启动或阶段完成。
- W3D3 正式主题为“用户与订单基础查询”，成果为 `basic-sql-queries.sql` 与 `basic-query-log.md`／“6 道基础查询”；首教边界限定为查询问题、WHERE 条件、比较符、文本值筛选、日期范围边界、排序方向、列别名和 DISTINCT 结果去重。
- W3D3 只教学本地 orders 表的单表筛选、排序、别名、DISTINCT 基础去重观察和日期条件，严格保留预测—操作—观察—记录—解释—对照流程，answers.sql 不提前暴露；不提前完成 W3D4 数据质量诊断、聚合、GROUP BY、JOIN、指标或真实业务查询集。W3D4 已在后续授权中单独完成，见下方记录。
- 2026-08-25：W3D4 作为用户另行授权的跨阶段单日插入已完成并进入 available，但阶段 03 仍为 `pending`，不代表 W3D5–W5D6 启动或阶段完成。
- W3D4 正式主题为“查询为何不可信”，成果为 `data-quality-checklist.md`／“数据质量检查表”；首教边界限定为数据质量问题、NULL 值、重复行、状态覆盖、测试数据标记、时间边界风险、金额异常和质量证据边界。
- W3D4 只教学本地 orders 表的行级质量风险检查：C1 缺用户、C2 缺支付时间、C3 重复 O1018、C4 测试订单、C5 0 元或空金额、C6 日期边界行；严格保留预测—操作—观察—记录—解释—对照流程，answers.sql 不提前暴露；不提前完成聚合、GROUP BY、JOIN、指标口径、生产数据审计或 W3D5 内容。W3D5 已在后续授权中单独完成，见下方记录。
- 2026-08-25：W3D5 作为用户另行授权的跨阶段单日插入已完成并进入 available，但阶段 03 仍为 `pending`，不代表 W3D6–W5D6 启动或阶段完成。
- W3D5 正式主题为“向数据研发确认口径”，成果为 `data-request.md`／“取数需求单”；首教边界限定为取数需求目的、业务问题范围、字段与来源假设、筛选规则契约、时间窗口契约、排除与标注规则、期望输出形状、复核问题与证据边界。
- W3D5 只把 W3D3 的基础查询和 W3D4 的质量风险转成数据研发可复核口径：R1 范围缺失、R2 字段来源缺失、R3 筛选规则缺失、R4 时间窗口歧义、R5 质量排除缺失、R6 合格取数需求；严格保留预测—操作—观察—记录—解释—对照流程，不连接真实数据库，不提交真实工单，不提前完成聚合、GROUP BY、JOIN、指标计算或 W3D6 业务查询集。W3D6 已在后续授权中作为单日插入完成，阶段 03 仍因 W4D1–W5D6 未完成保持 `pending`。
