---
stage: 01
status: active
scope: W1D4-W1D6
depends_on: W1D1-W1D3 available
---
# 阶段 01：完成 W1 请求链基础

## 功能

- W1D4 首次教 API 入口、后端职责、数据库持久化和请求处理边界；引导实验串起 HTTP→API→业务→数据库→响应；成果为请求处理链路说明。
- W1D5 首次教 Network 请求条目、Headers、Payload、Response、Timing、Initiator 与缓存线索；成果为有“看到/推断”边界的证据记录。
- W1D6 综合页面、寻址、HTTP、API/后端/数据层与浏览器证据，改变故障层做独立诊断；成果为最小可复现问题说明/链路报告。

## 约束

W1D4 不提前进入真实 Network、鉴权、并发、幂等或复杂 SQL；W1D5 不要求未首教的生产权限；W1D6 不凭单一状态码归因。三门课必须各有专属场景、实验、练习和成果，不复制 W1D1–W1D3。

2026-08-21 插入式任务：用户已亲自确认 Day 04；只处理 W1D5，将其实现为 Day 01 两时段七章框架下的 Network 证据观察课程，验证字段首教、七变式观察器、正式证据 bridge 与跨日提前观看；阶段 01 保持 `active`，不制作 W1D6。W1D5 随后于 2026-08-22 获用户亲自确认，相关单日任务停止；阶段 01 仍保持 `active`，W1D6 继续未制作。

### Day 02 已确认基线（2026-08-20）

- 旧独立落地页方案已推倒。`W1D2Page.vue` 与 `W1D2Day01CourseView.vue` 现复刻正式 W1D1 可见工作台：224px 左侧全局学习栏且“课程”活动、Day 01 同比例中栏、244px sticky 七章目录、860px 手机课程栏与移动目录；旧“返回课程路线＋30/45 分钟”工具栏已删除。
- 课程路线点击 W1D2 后留在正式应用工作台，只切换中间正文；路由不读取前一天完成/掌握状态。Day 02 只使用自己的 URL、域名、DNS、服务器地址、协议、端口和路径内容，保留原 DailyCourse、实验、草稿、历史尝试、复习与 schema v2 证据账本。
- `validate:day01-framework` 确认 7 章、2 时段、正式外壳、桌面/移动目录三项独立状态及重置撤销；`validate:day-routes` 确认可提前观看。
- 1440／1024／860／390／320px 已验证三栏/手机比例、目录 sticky、点击定位、移动目录、学习／练习／复述分项高亮、重置撤销、刷新恢复、单一纵向滚动及无横向溢出。
- `npm run validate:all` 与 `npm run build` 通过；W1D1 20/20 和五断点回归通过，两个冻结文件哈希未变；3/72 available 未变化，W1D3 renderer 未修改。
- 正式离线入口 7,239,780 bytes，SHA-256 `1adafdef2fe4b47ded552da9991016082c0c4d3b6fa197776c4fd08a84846ccf`，外部运行资源 0。Day 02 已于 2026-08-21 获用户亲自确认。

### Day 03 插入式任务结果（2026-08-21）

- W1D3 旧通用 `DailyLessonView` 独立落地页方案已推倒。`W1D3Page.vue` 与 `W1D3Day01CourseView.vue` 现复刻正式 W1D1 工作台：224px 左侧全局学习栏且“课程”活动、Day 01 同比例中栏、244px sticky 七章目录、860px 手机课程栏与无内部滚动的移动目录；不存在外置“返回路线＋30/45 分钟”工具栏。
- 七章只教授 HTTP Request／Response、Method、URL、Header、Cookie、Query、Payload、Status、Response code、Timing 与 Size；专属 HTTP 教学模拟明确标注“不发送真实请求”，已验证 200／PREVIEW_READY 与 422／INVALID_TYPE 对照、刷新恢复和清空归零。
- 课程路线点击 W1D3 后保留同一应用工作台，只切换正文；入口和深链不读取 W1D2 完成状态。12 个 DailyCourse 语义字段、草稿、章节访问、追加式历史尝试、证据账本和复习记录继续由 adapter／bridge 接回正式状态源。
- 1440／1024／860／390／320px 已验证外壳几何、七章、目录 sticky／点击定位、移动目录、单一主纵向滚动、页面无横向溢出、学习／练习／复述独立高亮、重置撤销与刷新恢复；W1D1／W1D2 五档回归通过，Day 01／Day 02 源文件和冻结参考哈希未变。
- `npm run validate:all` 与 `npm run build` 通过；库存仍为 3/72 available，W1D4+ 未创建或开放。正式离线入口 7,198,945 bytes，SHA-256 `1957422a1756a7f21e4025391c886ed4cd93ee0deb40698695199557790b34be`，外部运行资源 0。当前等待用户亲自确认 Day 03；确认前不得开始 W1D4。

Day 03 随后于 2026-08-21 获用户亲自确认。

### Day 04 插入式任务结果（2026-08-21）

- `W1D4Page.vue` 与 `W1D4Day01CourseView.vue` 复刻正式 W1D1 工作台：224px 左栏且“课程”活动、Day 01 同宽正文、244px sticky 七章目录、860px 手机形态与无内部纵向滚动的移动目录；课程路线只在当前工作台切换正文，不显示外置返回／时长工具栏。
- 两时段七章只讲 API 入口、请求处理器、后端业务服务、业务规则、数据库读写、持久化边界和 Response 组装。引导实验按 HTTP → API → 后端业务处理 → 数据库读写 → Response 串联，并强制完成合法写入、规则拒绝、数据库失败三条明确标注“教学模拟”的分支；成果为“请求处理链路说明”。
- 学习、练习、复述分别保存与高亮；当前位置与完成状态分开，重置即时撤销，刷新恢复。草稿、章节访问、追加式历史尝试、不可变证据与复习记录经 adapter／bridge 写回正式状态源；七个概念各排程 D1/D3/D7/D14/D30/D60，共 42 项。
- 课程路线与 `#/lesson/W1D4` 深链在 W1D3 未完成时仍能打开完整课程。最终静态 `index.html` 在 1440／1024／860／390／320px 的几何、七章、sticky／移动目录、定位、键盘、可访问名称、单一主滚动与无横向溢出通过；W1D1–W1D3 五视口与冻结哈希回归通过。
- 全部专项门禁、`npm run validate:all`、`vue-tsc --noEmit` 与 `npm run build` 通过。库存为 4/72 available，W1D5+ 仍 missing；正式离线入口 7,341,827 bytes，SHA-256 `1c4d293e1e1acdb7e6a46c35915d31759c8ace1800d3981e23de77d211eca626`，外部运行资源 0。当前等待用户亲自确认 Day 04；确认前不得开始 W1D5。

Day 04 随后于 2026-08-21 获用户亲自确认。

### Day 05 插入式任务结果（2026-08-21）

- `W1D5Page.vue` 与 `W1D5Day01CourseView.vue` 复刻正式 W1D1 工作台外壳：224px 左栏且“课程”活动、Day 01 同宽正文、244px sticky 七章目录、860px 手机形态与无内部纵向滚动的移动目录；课程路线和深链都在同一工作台切换正文。
- 两时段七章逐项首教 Network 请求条目、Name／Request URL、Method、Status、Type、Initiator、Headers、Payload、Response、Timing、Size、缓存线索及“看到／可以推断／还不能证明／下一步”证据边界；没有复制 Day 03 消息模拟或 Day 04 后端／数据库处理链。
- 专属教学观察器必须累计完成正常、4xx、5xx、慢 Timing、Size 变化、Initiator 变化和缓存变式，且所有数据明确标为本地脱敏教学模拟；Network 表格只在具名容器内横向浏览。
- 草稿、章节访问、追加式历史尝试、证据账本和 9 个首教概念的 54 项复习任务通过 bridge 写回正式状态源；W1D4 未完成时，课程路线和 `#/lesson/W1D5` 仍可打开完整课程。
- `npm run validate:w1d5`、课程内容／实现／路由／证据／Day 01 框架／W1D1 冻结专项门禁通过；库存为 5/72 available，W1D6 仍 missing。当前等待用户亲自确认 Day 05；确认前不得开始 W1D6。
- 最终 `npm run validate:all` 和 `npm run build` 通过；专项门禁覆盖 1440／1024px 桌面几何合同、860／390／320px 手机合同、sticky／移动目录、单一主纵向滚动、唯一 Network 表格横向滚动、目录定位、键盘可达、可访问名称、分项状态与全部清空重做路径；9 个首教概念全部进入实验、练习、成果和复述。
- 证据 bridge 在 `pagehide` 与组件离开前同步写回最后一份防抖草稿；历史尝试保持追加，9 概念产生 D1／D3／D7／D14／D30／D60 共 54 个复习任务。`#/lesson/W1D5` 往返解析为当前工作台日课，route state 明确 `requiresPriorDayCompletion: false`。
- 本轮工具的 `file://` 安全协议拒绝打开批准参考与最终离线页；未绕过协议启动 localhost／Playwright／CDP，也未将没有执行的真实浏览器五视口交互冒充为通过。最终离线入口为 7,477,010 bytes，SHA-256 `3781301f3d127c853259364998a2b6d37507426d0fba1d716ee969d98ea090ba`，外部运行资源 0。

Day 05 随后于 2026-08-22 获用户亲自确认；阶段 01 保持 `active`，W1D6 未开始。

## 验收

- inventory available 为 6，包含 W1D1–W1D6。
- 首次教学顺序正确，概念无未来依赖，实验能/不能证明边界完整。
- 重置、刷新、复习、应用内路线和证据保存可用；W1D2 不显示独立“返回路线＋30/45”工具栏。
- W1D2 主目录恰好七章，12 个语义字段映射完整；清空 W1D1 完成状态后仍可从课程路线和 `#/lesson/W1D2` 打开完整课程。
- W1D3 主目录恰好七章，HTTP 专属内容与教学模拟不复制前两天；即使 W1D2 未完成，也能从课程路线和 `#/lesson/W1D3` 打开完整课程。
- W1D4 主目录恰好七章，API／后端／数据库专属内容及三条教学模拟不复制前三天；即使 W1D3 未完成，也能从课程路线和 `#/lesson/W1D4` 打开完整课程。
- `npm run validate:all`、`npm run build` 通过；1440/1024/860/390/320px 与 W1D1 回归通过。
- W1D1 回归按 2026-08-20 正式契约执行：顶部预览工具四类文案不可见，隐藏兼容按钮保留；1440/1024px 为桌面三栏，860/390/320px 为原手机形态；课程正文、左右导航和 7 章路线不变且无横向溢出。

## 退出

完成后将阶段 02 改为 `active`；身份、日志、环境和恢复知识留到 W2。
